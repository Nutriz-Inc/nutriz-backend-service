services:
  database:
    image: postgres:16
    container_name: database
    ports:
      - 5435:5432
    environment:
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=nutriz

  # redis:
  #   container_name: redis
  #   image: redis:latest
  #   restart: always
  #   ports:
  #     - 6398:6379
  #   environment:
  #     - ALLOW_EMPTY_PASSWORD=yes